# TODO igloo-boot: unifying database config
spring.datasource.url=jdbc:postgresql://${TEST_DB_HOST:localhost}:${TEST_DB_PORT:5436}/${TEST_DB_NAME:igloo_test}
spring.jpa.properties.hibernate.default_schema=${TEST_DB_SCHEMA:igloo_test}
spring.datasource.username=${TEST_DB_USER:igloo_test}
spring.datasource.password=${TEST_DB_PASSWORD:igloo_test}
spring.datasource.driver-class-name=org.postgresql.Driver