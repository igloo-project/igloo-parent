#
# jpa-test specific configuration
#

# to allow TestCache on Company
hibernate.cache=jcache-caffeine
hibernate.jcache.configurationLocation=classpath://caffeine.conf

# we need to create schema
hibernate.hbm2ddl.auto=create