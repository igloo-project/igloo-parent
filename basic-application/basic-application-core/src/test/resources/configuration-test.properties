# this property is preloaded to control configuration loading. It needs to be defined in this file.
environment=${maven.environment}

# Database
db.type=${maven.test.db.type}
db.jdbcUrl=${maven.test.db.jdbcUrl}
db.user=${maven.test.db.user}
db.password=${maven.test.db.password}
db.maxPoolSize=${maven.test.db.maxPoolSize}

hibernate.hbm2ddl.auto=${maven.test.hibernate.hbm2ddl.auto}

# Paths
data.path=${maven.test.data.path}
shared-data.path=${maven.test.data.path}
test.data.path=${maven.test.data.path}