scope=fr.openwide.core.basicapp.core,fr.openwide.core,java.math,java.util,java.lang
skipBindKeyword=true
bindingPathSuperClass=fr.openwide.core.commons.util.binding.AbstractCoreBinding
# Bindgen will generate code that won't compile for these attributes.
# However, we don't need these attributes, so we'll just work around the issue by ignoring them.
skipAttribute.fr.openwide.core.jpa.business.generic.model.GenericEntityReference.entityClass=true
skipAttribute.fr.openwide.core.commons.util.fieldpath.FieldPath.root=true
